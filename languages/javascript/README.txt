In no way, shape, or form ready for prime time. The idea is to 
do javascript bindings for DIS. This is a somewhat dodgy 
enterprise, since Javascript doesn't deal well with binary 
at all, though this may change in future versions.

Rather, the idea is to use JSON to ship the data contained
in DIS packets around via the web.


